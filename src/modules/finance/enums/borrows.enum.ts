export enum BorrowType {
  BORROW = 'borrow',
  LEND = 'lend',
}

export enum BorrowsStatus {
  ACTIVE = 'active',
  RETURNED = 'returned',
  OVERDUE = 'overdue',
}

export enum BorrowParty {
  SUPPLIER = 'supplier',
  CLIENT = 'client',
  OTHER = 'other',
}
